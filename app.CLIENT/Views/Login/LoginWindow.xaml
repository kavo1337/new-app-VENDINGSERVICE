<Window x:Class="app.CLIENT.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:app.CLIENT"
        mc:Ignorable="d"
        Title="Login"
        Height="360"
        Width="520"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="WSR" FontSize="28" FontWeight="Bold"/>
            <TextBlock Text="Вход в аккаунт" Foreground="Gray"/>
        </StackPanel>

        <StackPanel Grid.Row="1" VerticalAlignment="Top" Margin="0,0,0,16">
            <TextBlock Text="Электронная почта" Margin="0,0,0,4"/>
            <TextBox x:Name="EmailBox" Height="30"/>

            <TextBlock Text="Пароль" Margin="0,12,0,4"/>
            <PasswordBox x:Name="PasswordBox" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Button x:Name="LoginButton" Height="36" Content="Войти" Click="LoginButton_Click"/>
            <TextBlock x:Name="StatusText" Margin="0,8,0,0" Foreground="Gray" />
        </StackPanel>
    </Grid>
</Window>
